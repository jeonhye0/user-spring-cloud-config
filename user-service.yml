spring:
  application:
    name: user-service
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password:

token:
  expiration_time: 86400000
  secret: oopsw_com_key_oopsw_com_key_user_service

order_service:
  url: http://order-service/order-service/%s/orders
  exception:
    orders_is_empty: user's orders is empty check
gateway:
  ip: 172.18.0.5
